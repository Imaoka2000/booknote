<h1>ユーザー設定</h1>

<ul>
  <li><%= link_to "プロフィール編集", edit_user_path(current_user), data: { turbo_frame: "_top" } %></li>
  <li><%= link_to "アカウント設定", edit_user_settings_path, data: { turbo_frame: "_top" } %></li>
</ul>

<hr>

<!-- アカウント削除フォーム -->
<div style="margin-top:2em;">
  <h3>アカウント削除</h3>
  <p>この操作は元に戻せません。ご注意ください。</p>
  <%= form_with url: user_settings_path, method: :delete, data: { turbo_confirm: "本当に削除しますか？この操作は元に戻せません。" }, local: true do %>
    <%= submit_tag "アカウントを削除する", class: "btn btn-danger" %>
  <% end %>
</div>
